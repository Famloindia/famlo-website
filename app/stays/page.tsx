import { ResetPage } from "@/app/_components/ResetPage";

export default function StaysPage() {
  return (
    <ResetPage
      title="Stays page reset."
      description="The combined stays page has been cleared as well, so it’s ready for a fresh rebuild."
    />
  );
}
