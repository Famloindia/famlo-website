import { ResetPage } from "@/app/_components/ResetPage";

export default function AdminProfilePage() {
  return (
    <ResetPage
      title="Admin profile page reset."
      description="This dynamic admin profile route has been cleared and can be rebuilt later if it still fits the new site."
    />
  );
}
