import { ResetPage } from "@/app/_components/ResetPage";

export default function HomeDetailPage(): JSX.Element {
  return (
    <ResetPage
      title="Home detail page reset."
      description="Individual home pages have been cleared too, so every detail route now starts from a blank baseline."
    />
  );
}
