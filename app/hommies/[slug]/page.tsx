import { ResetPage } from "@/app/_components/ResetPage";

export default function HommieDetailPage(): JSX.Element {
  return (
    <ResetPage
      title="Hommie detail page reset."
      description="Every hommie detail route now points to a clean placeholder while we rebuild the site in stages."
    />
  );
}
