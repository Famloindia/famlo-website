import { ResetPage } from "@/app/_components/ResetPage";

export default function FriendsPage(): JSX.Element {
  return (
    <ResetPage
      title="Friends page reset."
      description="The old friends listing has been cleared so we can rebuild this route with a fresh structure."
    />
  );
}
